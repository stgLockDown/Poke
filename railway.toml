# Railway deployment config
# https://docs.railway.app/reference/config-as-code

[deploy]
startCommand = "python run.py run"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10